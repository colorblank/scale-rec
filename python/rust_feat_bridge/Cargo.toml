[package]
name = "rust_feat_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "feat_engine"

[dependencies]
scale-rec-core = { path = "../../crates/core" }
pyo3 = { version = "0.21", features = ["extension-module"] }
